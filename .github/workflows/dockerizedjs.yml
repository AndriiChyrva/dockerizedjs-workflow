name: dockerizedjs-workflow
run-name: ${{github.actor}} is running dockerizedjs-action
on:
  workflow_dispatch
jobs:
  dockerizedjs-job:
    runs-on: ubuntu-latest
    steps:
      # - id: add_problem_matcher
        # run: "echo ::add-matcher::/problem-matcher.json"
        # run: "echo ::add-matcher::./../../../../../action/dist/problem-matcher.json"
      - id: run_action
        uses: AndriiChyrva/d-checkout@v1.0.58